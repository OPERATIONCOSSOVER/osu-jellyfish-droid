package com.osudroid.difficulty.taiko

/**
 * The colour of a taiko hit.
 */
enum class HitType {
    /**
     * A centre hit, played on the drum skin (don).
     */
    Centre,

    /**
     * A rim hit, played on the drum rim (kat).
     */
    Rim
}
